#include <stdio.h>

int
main ()
{
  long long sum = 0;
  for (int i = 0; i < 1000; i++)
    {
      if (i % 3 == 0 || i % 5 == 0)
	    {
	      sum += i; //if number is a multiple of 3 or 5, add it to the sum.
	    }
    }

  printf ("%lld", sum);
  return 0;
}
